# NOCTIS — LEDGER

One row per session. Created in session 19; sessions 1–18 predate it and are not
reconstructed, because a cost invented after the fact is worse than a gap.

**What the columns mean.**

- **wall** — first tool call to last. An hour waiting for a quiet machine costs
  no tokens and a great deal of clock; recording both is the point.
- **in / cache read / out** — as `/cost` reports, in millions to two decimals.
  Cache reads have their own column because they are the only number the
  operator can influence, and they are priced at a tenth of fresh input.
- **USD** — **computed, not copied**: `in × $5 + cache read × $0.50 + out × $25`.
  Where `/cost` reports something different, both are recorded and the row says
  which is which.
- **SEK** — with the rate, in a footnote.
- **kind** — one of `build`, `diagnose`, `measure`, `content`, `tooling`, `mixed`.
- **bought** — one line, plain, what the session produced. Not what it attempted.

| #  | date  | wall | in | cache read | out | USD | SEK | kind | bought |
|----|-------|------|----|------------|-----|-----|-----|------|--------|
| 19 | 2026-08-10 | 1 h 40 m | — | — | — | — | — | mixed | The ground datum declared and read by everything that stands on it; the streamed kerb 0.010 → 0.160 m and the 98.5 m pavement step to zero; the half-metre road slabs in the carriageway found and fixed; traffic stopping at its own line with the gate for it built; the lamp bounce into the canyon field, and the arithmetic proving it cannot pay for the bowl correction; the condenser floodlit and 18 aviation beacons; a roofscape of five kinds, on the ring that is the skyline; a map, a time menu, fullscreen, a fly camera and walk at 3.5 m/s; CONTRACT §9's row count generated by a gate instead of maintained in prose. |
| 20 | 2026-08-11 | 2 h 05 m | — | — | — | — | — | mixed | Roof signs — 262 of them over a 10 x 10 chunk region, taking the delivered sign quads from 578 to 1 100 — at `LIGHT.roofSignNits` = 1000 (ILP GN01 E4), on three mountings, at zero draw calls and zero cluster slots — the elevated night frame's crushed fraction 5.86% → 0.00% and its textured band 1.64% → 6.52%; a log-normal height distribution at sd/mean 0.664 against 0.425 with setbacks, tiered massing, tiered facades and a parapet on every step; six aircraft with regulation navigation lights and one helicopter with a searchlight, at 2 draws and 1 cluster slot; a four-level HUD that measures the rAF callback, subtracts its own cost and refuses to print an EV §5.4 forbids reading; and the `player` route's breach attributed — paired and interleaved — to its 6000-frame measurement window at +1.17 ± 0.07 ms, with its camera measured 0.70 ms CHEAPER than `downtown_dense`'s. |
| 21 | 2026-08-11 | 1 h 25 m | — | — | — | — | — | mixed | One keep-out registry — 12 categories, 47 forbidden pairs, a vertical extent on every claim — written and read by every generator and asserted over the DELIVERED scene, taking the forbidden overlaps over the gate's own region from 200 to 0; the viaduct on portal frames with 0 of 44 legs in a carriageway against 8 of 23 piers before, terminating on abutments after the carried diagnosis was measured wrong (an arc length used as a straight-line extent), and carrying two trains; parks with a path network, clumped planting, an 870 cd / 4.2 m lighting scheme, an edge and a centre; construction sites as a fifth low-detail kind with a slewing crane and 45 000 cd flood masts; four tree species of overlapping tilted canopy masses; 13 593 road markings painted from the same `stopLineFromJunctionM` the traffic brakes against; the stop-line assertion carried since session 19; the three level assertions pooled; and `citycheck` made to deliver every placement verdict on a machine with no GPU instead of none. |

**Session 20's cost cells are empty for session 19's reason**, unchanged: `/cost`
is interactive and this session ran non-interactively. The wall figure is real —
07:06 to 09:11 local, first tool call to last.

**Machine conditions for session 20.** `load1` ran 2.3–4.2 for most of the
session and **1.75 → 1.95 across the final gate battery**, against the CONTRACT
§0.2 quiet bar of **1.6**. No orphan vite, chromium or playwright processes were
resident at the start and none was left. Seven of eight gates are green;
`perfcheck` is red on two assertions and neither is a verdict at 1.75. The counts
in the same run — draws, triangles, instances, froxel occupancy, light roles —
are integers and do not drift, so they are evidence.

**Session 19's cost cells are empty, and the reason is not an omission.** `/cost`
is an interactive CLI command and this session ran non-interactively, so no token
figure was available to record. Per the rule this ledger was created with — *a
gap with a reason beats a guess* — the cells stay empty rather than being
estimated. The wall figure is real: 22:20 to 00:00 local, measured from the first
tool call to the last.

**Machine conditions for session 19, because they bound what its numbers mean.**
`load1` ran 3.4–4.2 throughout against the CONTRACT §0.2 quiet bar of **1.6**,
with the Claude app resident and two orphan vite servers from earlier sessions
that the environment refused to let the session kill. Seven of eight gates are
green; `perfcheck`'s six timing assertions are red and **none of them is a
verdict** at that load. The counts in the same run — draws, triangles, instances,
froxel occupancy — are integers and do not drift, so they are evidence.

**Session 21's cost cells are empty for the same reason as 19 and 20**, and the
reason is unchanged: `/cost` is an interactive CLI command and this session ran
non-interactively, so no token figure was available to record. Per the rule this
ledger was created with — *a gap with a reason beats a guess* — the cells stay
empty rather than being estimated. The wall figure is real: 09:04 to 10:29 UTC,
first tool call to last.

**Machine conditions for session 21, and they are categorically different from
every session before it.** THIS SESSION DID NOT RUN ON THE OPERATOR'S COMPUTER.
It ran in a Linux container with no GPU, on a SwiftShader software rasteriser.
`faultcheck`, `lookcheck` and `windcheck` refuse a software renderer outright
and produced no verdict; `citycheck`'s saturation sample reports UNRUN and the
gate exits 1; every millisecond `perfcheck` prints is a measurement of
SwiftShader and is not evidence about anything. What IS evidence is every count,
coordinate and conflict in STATE.md — CONTRACT §9 rule 6's corollary, that
counts do not drift, is the whole reason this session was measurable at all.
**No quiet bar was derived and `budget.json`'s `machine` field is untouched**:
deriving one here would have recorded a Linux VM's floor as an M4's or an M5's,
which is §9's failure mode with a computer instead of a length.
